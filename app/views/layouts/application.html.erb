<!DOCTYPE html>
<html lang="en" data-ng-app="calcentral" data-ng-controller="CalcentralController">
<head>
  <meta charset="utf-8" />
  <!-- Set the viewport width to device width for mobile -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="CalCentral is a new resource for the UC Berkeley community." />
  <%= csrf_meta_tags %>

  <title data-ng-bind="title">CalCentral</title>

  <noscript><meta http-equiv="Refresh" content="0; URL=/enable-javascript.html"></noscript>

  <link rel="icon" href="/assets/favicon.png" type="image/png" />
  <link rel="shortcut icon" href="/assets/favicon.ico" />

  <!-- Included CSS Files -->
  <%= stylesheet_link_tag "application" %>

  <!-- Included JS Files  -->
  <%= javascript_include_tag "application" %>
</head>

<body
  data-ng-class="{'cc-page-splash':(controller_name=='SplashController')}"
  data-ng-click="api.popover.closeAll()"
  data-ng-show="user.isLoaded">

  <%= render 'templates/ieold' %>
  <header class="row">
    <!-- TODO: figure out a way to set a default for no matched yield blocks -->
    <%= yield(:header) %>
  </header>
  <%= render 'templates/navigation' %>

  <div class="row cc-container-main" role="main">
    <%= yield %>
  </div>

  <%= render 'templates/footer' %>
  <%= render 'templates/all' %>

</body>
</html>
