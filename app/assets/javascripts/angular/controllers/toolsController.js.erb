<% if Rails.env == 'development' %>
(function(calcentral) {
  'use strict';

  /**
   * Styles controller
   */
  calcentral.controller('ToolsController', ['apiService', '$http', '$routeParams', '$scope', function(apiService, $http, $routeParams, $scope) {

  }]);

})(window.calcentral);
<% end %>
