<% if Rails.env == 'development' %>
(function(calcentral) {
  'use strict';

  /**
   * Tools controller
   */
  calcentral.controller('ToolsController', angular.noop);

})(window.calcentral);
<% end %>
