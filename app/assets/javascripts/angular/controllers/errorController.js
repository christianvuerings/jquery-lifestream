(function() {
  /*global calcentral*/
  'use strict';

  /**
   * Error controller
   */
  calcentral.controller('ErrorController', ['$rootScope', function($rootScope) {

    $rootScope.title = 'Error | CalCentral';

  }]);

})();
