(function() {
  /*global calcentral*/
  'use strict';

  /**
   * Splash controller
   */
  calcentral.controller('SplashController', ['$rootScope', function($rootScope) {

    $rootScope.title = 'Home | CalCentral';

  }]);

})();
