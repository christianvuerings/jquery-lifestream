(function(angular) {
  'use strict';

  /**
   * Financial Aid controller
   */
  angular.module('calcentral.controllers').controller('FinaidController', function(apiService) {
    apiService.util.setTitle('Financial Aid');
  });
})(window.angular);
